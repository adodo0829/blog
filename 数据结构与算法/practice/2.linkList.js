// 链表题
const linklist = require("../structure/4.linkedList").LinkList;
console.log(linklist.toString());

/**
 * 1.反转链表
 */
const reverseLinkList = (list) => {
  let curr = list.head;
};

console.log(linklist.toString());
