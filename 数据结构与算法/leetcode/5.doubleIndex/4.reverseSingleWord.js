// 给定一个字符串，逐个翻转字符串中的每个单词。
// 输入: " the sky is blue "
// 输出: "blue is sky the"

// 移除多余空格
// 翻转整个string
// 翻转单个string

var reverseWord = (s) => {
  const strArr = Array.from(s);

  // 怎么移除数组元素, 将有效元素往前移动, 最后截断, 给length赋值
  const removeSpace = (arr) => {
    // 需要对首位空格做处理
    let fast = 0
    let slow = 0
  };

  const reverseStrArr = (arr, i, j) => {
    let right = j
    let left = i
    // ...
  }

  // arr局部翻转

};
